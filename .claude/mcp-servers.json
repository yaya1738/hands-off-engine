{
  "mcpServers": {
    "github": {
      "command": "github-mcp-server",
      "args": [],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub MCP Server - Git operations and repository management (30+ operations auto-discovered)",
      "notes": "Tools are discovered dynamically via stdio. Claude Code has direct access. Other AIs (ChatGPT) access via AI Nexus coordination layer."
    },
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp"],
      "env": {},
      "description": "Playwright MCP Server - Web browser automation and testing (tools auto-discovered)",
      "notes": "Tools are discovered dynamically via stdio. Claude Code has direct access. Other AIs access via AI Nexus coordination layer."
    }
  },
  "configuration": {
    "github": {
      "notes": "Requires GITHUB_TOKEN environment variable",
      "setup_steps": [
        "1. Create a GitHub Personal Access Token: https://github.com/settings/tokens",
        "2. Set GITHUB_TOKEN environment variable: export GITHUB_TOKEN=your_token_here",
        "3. Or store in .claude/.env file (do not commit)"
      ]
    },
    "playwright": {
      "notes": "Requires Node.js and npm",
      "setup_steps": [
        "1. Playwright MCP server is already installed via: npm install -g @playwright/mcp",
        "2. Ensure Node.js is available: node --version",
        "3. No additional configuration needed"
      ]
    }
  }
}
