# then restart:
pkill -f "python $HOME/hands-off/autopilot/admin_server.py" 2>/dev/null || true
bash "$HOME/hands-off/autopilot/start_admin.sh"# optional; if empty, admin stays local-open
ADMIN_TOKEN=
ADMIN_PORT=8787
