# LLM Integration Overview

## Purpose

The LLM intelligence layer provides AI-driven market analysis for the Hands-Off trading system. It augments the existing Polymarket pipeline with sophisticated probability estimates, confidence levels, and reasoning from state-of-the-art language models.

## Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Polymarket Alpha Pipeline                     │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
                  ┌───────────────────────┐
                  │  polymarket-compact   │
                  │        .json          │
                  └───────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────────┐
│                   LLM Intelligence Layer                         │
│                                                                  │
│  ┌────────────────┐   ┌─────────────────┐   ┌───────────────┐  │
│  │ Backend        │   │ Prompt          │   │ Market        │  │
│  │ Selector       │──▶│ Library         │──▶│ Analyst       │  │
│  │                │   │                 │   │               │  │
│  │ - Claude API   │   │ - Politics      │   │ - DRYRUN mode │  │
│  │ - OpenRouter   │   │ - Crypto        │   │ - Validation  │  │
│  │ - Local models │   │ - Sports        │   │ - Fallback    │  │
│  │ - Fallback     │   │ - Macro         │   │               │  │
│  └────────────────┘   └─────────────────┘   └───────────────┘  │
│                                                     │            │
└─────────────────────────────────────────────────────┼────────────┘
                                                      ▼
                                         ┌────────────────────────┐
                                         │  llm_alpha_report      │
                                         │       .json            │
                                         └────────────────────────┘
                                                      │
                                                      ▼
                                         ┌────────────────────────┐
                                         │   Downstream Scoring   │
                                         │   & Decision Layer     │
                                         └────────────────────────┘
```

## Pipeline Flow

### 1. Input: `polymarket-compact.json`

**Location:** `~/hands-off-out/state/polymarket-compact.json`

**Format:**
```json
{
  "markets": [
    {
      "id": "market-001",
      "question": "Will Bitcoin reach $100k in 2025?",
      "category": "crypto",
      "yes_price": 0.62,
      "no_price": 0.38,
      "volume": 500000,
      "closes_at": "2025-12-31T23:59:59Z"
    },
    {
      "id": "market-002",
      "question": "Will the Senate pass the infrastructure bill?",
      "category": "politics",
      "yes_price": 0.55,
      "no_price": 0.45,
      "volume": 100000,
      "closes_at": "2025-03-01T00:00:00Z"
    }
  ]
}
```

**Source:** Generated by `alpha/ho_alpha_polymarket.py` or `termux-hands-off/agent/pm_trim.py`

---

### 2. Processing: LLM Analysis

**Script:** `llm/ho_llm_polymarket.py`

**Steps:**
1. **Load markets** from input JSON
2. **Initialize LLMMarketAnalyst** with DRYRUN mode (default)
3. **For each market:**
   - Select appropriate LLM backend (Claude API, OpenRouter, or fallback)
   - Route to category-specific prompt (politics, crypto, sports, macro, generic)
   - Call LLM API (or simulate in DRYRUN mode)
   - Parse and validate JSON response
   - Fall back to naive model if LLM unavailable
4. **Aggregate results** with metadata

---

### 3. Output: `llm_alpha_report.json`

**Location:** `~/hands-off-out/state/llm_alpha_report.json`

**Format:**
```json
{
  "generated_at": "2025-11-18T12:34:56.789Z",
  "analyst_status": {
    "dry_run": true,
    "has_llm_backend": false,
    "available_backends": [],
    "fallback_enabled": true
  },
  "total_markets": 2,
  "llm_analyzed": 0,
  "fallback_count": 2,
  "markets": [
    {
      "id": "market-001",
      "question": "Will Bitcoin reach $100k in 2025?",
      "category": "crypto",
      "yes_price": 0.62,
      "volume": 500000,
      "closes_at": "2025-12-31T23:59:59Z",
      "llm_analysis": {
        "fair_probability": 68.5,
        "confidence": "medium",
        "edge_bps": 650,
        "reasoning": "Simulated analysis for DRYRUN mode. Market at 62.0%, estimate 68.5%.",
        "action": "buy_yes",
        "backend_used": "claude-api",
        "model_name": "claude-api",
        "prompt_version": "v1.0.0",
        "tokens_used": null
      },
      "analysis_source": "llm"
    },
    {
      "id": "market-002",
      "question": "Will the Senate pass the infrastructure bill?",
      "category": "politics",
      "yes_price": 0.55,
      "volume": 100000,
      "closes_at": "2025-03-01T00:00:00Z",
      "llm_analysis": null,
      "analysis_source": "fallback_naive"
    }
  ]
}
```

**Schema:**

| Field | Type | Description |
|-------|------|-------------|
| `generated_at` | ISO timestamp | When analysis was run |
| `analyst_status` | object | LLM backend availability and mode |
| `total_markets` | integer | Total markets analyzed |
| `llm_analyzed` | integer | Markets analyzed with LLM |
| `fallback_count` | integer | Markets using fallback (no LLM) |
| `markets` | array | Per-market analysis results |

**Per-market schema:**

| Field | Type | Description |
|-------|------|-------------|
| `id` | string | Market identifier |
| `question` | string | Market question |
| `category` | string | Market category (crypto, politics, etc.) |
| `yes_price` | float | Current YES price (0-1) |
| `volume` | float | Market volume in USD |
| `closes_at` | string | Resolution timestamp |
| `llm_analysis` | object or null | LLM opinion (null if unavailable) |
| `analysis_source` | string | "llm" or "fallback_naive" |

**LLM Analysis object:**

| Field | Type | Description |
|-------|------|-------------|
| `fair_probability` | float | LLM's estimate of YES probability (0-100%) |
| `confidence` | string | "low", "medium", or "high" |
| `edge_bps` | integer | Edge in basis points vs market price |
| `reasoning` | string | 1-2 sentence explanation |
| `action` | string | "buy_yes", "buy_no", or "avoid" |
| `backend_used` | string | Which LLM backend was used |
| `model_name` | string | Specific model identifier |
| `prompt_version` | string | Prompt template version |
| `tokens_used` | integer or null | Token count (if available) |

---

## Usage

### Command Line

```bash
# Basic usage (DRYRUN mode, reads from default paths)
python llm/ho_llm_polymarket.py

# Custom input/output paths
python llm/ho_llm_polymarket.py \
  --input ~/hands-off-out/state/polymarket-compact.json \
  --output ~/hands-off-out/state/llm_alpha_report.json

# Limit number of markets (for testing)
python llm/ho_llm_polymarket.py --max-markets 10

# Use speed-optimized backends
python llm/ho_llm_polymarket.py --priority speed

# Enable real API calls (requires API keys)
python llm/ho_llm_polymarket.py --no-dryrun
```

### Python API

```python
from llm import LLMMarketAnalyst
from llm.backend_selector import Priority

# Initialize analyst
analyst = LLMMarketAnalyst(
    default_priority=Priority.QUALITY,
    enable_fallback=True,
    dry_run=True,  # DRYRUN mode
)

# Analyze a single market
market = {
    "question": "Will Bitcoin reach $100k?",
    "category": "crypto",
    "yes_price": 0.62,
    "volume": 500000,
    "closes_at": "2025-12-31",
}

opinion = analyst.analyze_market(market)

if opinion:
    print(f"Fair probability: {opinion.fair_probability}%")
    print(f"Edge: {opinion.edge_bps} bps")
    print(f"Action: {opinion.action}")
    print(f"Reasoning: {opinion.reasoning}")
else:
    print("LLM analysis unavailable, use fallback")
```

---

## Error Handling & Safety

### DRYRUN Mode (Default)

- **Enabled by default** for safety
- **Simulates LLM calls** without real API requests
- **Returns realistic synthetic responses** for testing
- **No API costs** or rate limiting
- **Logs all operations** for audit trail

### Graceful Degradation

1. **No API keys configured** → Falls back to `analysis_source: "fallback_naive"`
2. **API call fails** → Returns `null` for `llm_analysis`
3. **Invalid LLM response** → Logs error, returns `null`
4. **Malformed market data** → Handles missing fields gracefully

### Validation

All LLM responses are validated against schema:
- `fair_probability` must be 0-100
- `confidence` must be "low", "medium", or "high"
- `action` must be "buy_yes", "buy_no", or "avoid"
- All required fields must be present

Invalid responses are rejected and logged.

---

## Integration with Existing Pipeline

### Current State

The LLM layer is **standalone and DRYRUN-only**. It produces analysis output but does **not** affect trading decisions or execution.

### Future Integration Path

```
polymarket-compact.json
        │
        ├──→ [EXISTING] alpha/ho_alpha_polymarket.py → naive model scoring
        │
        └──→ [NEW] llm/ho_llm_polymarket.py → LLM scoring
                    │
                    ▼
              llm_alpha_report.json
                    │
                    ▼
              [FUTURE] Combined scoring layer
                    │    (weighted ensemble: naive + LLM)
                    ▼
              decider/ho_decider.py
                    │
                    ▼
              executor/ho_executor_plan.py (DRYRUN)
```

### Recommended Integration Strategy

1. **Phase 1 (Current):** LLM runs in parallel, produces separate output for analysis
2. **Phase 2:** Add evaluation comparing LLM vs naive model performance
3. **Phase 3:** Implement ensemble scoring (weighted average)
4. **Phase 4:** Use ensemble scores in decider (DRYRUN only)
5. **Phase 5:** Enable LIVE execution after validation period

---

## Configuration

### Environment Variables

| Variable | Purpose | Required |
|----------|---------|----------|
| `ANTHROPIC_API_KEY` | Claude API access | Optional (enables Claude backend) |
| `OPENROUTER_API_KEY` | OpenRouter proxy access | Optional (enables OpenRouter backends) |

Without API keys, the system falls back to naive model simulation.

### Backend Priority

Choose backend priority based on use case:

| Priority | Use Case | Backend Selection |
|----------|----------|-------------------|
| `quality` | Best accuracy | Claude API → OpenRouter Claude → GPT-4 |
| `speed` | Low latency | OpenRouter Llama → GPT-4 → Claude |
| `cost` | Minimize spend | Local models → OpenRouter Llama → Fallback |

---

## Testing

### Unit Tests

```bash
# Test backend selection logic
pytest tests/unit/test_backend_selector.py -v

# Test prompt generation
pytest tests/unit/test_prompt_library.py -v

# Test market analyst
pytest tests/unit/test_market_analyst.py -v
```

### Integration Tests

```bash
# Test full pipeline
pytest tests/integration/test_llm_pipeline.py -v

# Run all LLM tests
pytest tests/ -k llm -v
```

All tests are hermetic and use DRYRUN mode (no real API calls).

---

## Performance & Costs

### Token Usage (Estimated)

| Component | Tokens |
|-----------|--------|
| Base prompt | ~150 |
| Category prompt | ~250 |
| Market data | ~50 |
| LLM response | ~150 |
| **Total per market** | **~600** |

### API Costs (Approximate)

| Backend | Cost per 1k tokens | Cost per market |
|---------|-------------------|-----------------|
| Claude API | $0.015 | ~$0.009 |
| OpenRouter GPT-4 | $0.030 | ~$0.018 |
| OpenRouter Llama | $0.0001 | ~$0.00006 |

For 100 markets: ~$0.90 (Claude), ~$1.80 (GPT-4), ~$0.006 (Llama)

### Latency (Estimated)

| Backend | Avg latency per market |
|---------|------------------------|
| Claude API | ~2 seconds |
| OpenRouter GPT-4 | ~1.5 seconds |
| OpenRouter Llama | ~0.8 seconds |
| DRYRUN simulation | ~0.01 seconds |

---

## Troubleshooting

### LLM analysis always returns `null`

**Cause:** No API keys configured or DRYRUN mode with backend unavailable

**Solution:**
1. Set `ANTHROPIC_API_KEY` or `OPENROUTER_API_KEY`
2. Or use DRYRUN mode (default) for testing without API keys

### "No backends available" warning

**Cause:** API keys not set in environment

**Solution:**
```bash
export ANTHROPIC_API_KEY="your-key-here"
# or
export OPENROUTER_API_KEY="your-key-here"
```

### Invalid JSON in LLM response

**Cause:** LLM produced non-JSON output

**Solution:** Logged and handled automatically. System falls back to `null` analysis.

---

## Maintenance

### Updating Prompts

1. Edit prompts in `llm/prompt_library.py`
2. Increment `PROMPT_VERSION`
3. Run tests: `pytest tests/unit/test_prompt_library.py`
4. Commit changes

### Adding New Backend

1. Add to `Backend` enum in `llm/backend_selector.py`
2. Add `BackendConfig` to `BACKEND_REGISTRY`
3. Implement API call logic in `LLMMarketAnalyst._call_llm_api()`
4. Update tests

### Monitoring

Check `llm_alpha_report.json` for:
- `llm_analyzed` count (should be high if API keys configured)
- `fallback_count` (should be low if LLM working)
- `analyst_status.available_backends` (should list configured backends)

---

## Security

### API Keys

- **Never commit API keys** to the repository
- Store in environment variables only
- Use `.env` file with proper permissions (not committed)

### Rate Limiting

- System respects backend rate limits
- Gracefully degrades to fallback on quota exhaustion
- No automatic retry loops (prevents runaway costs)

### DRYRUN Safety

- **DRYRUN mode is default** to prevent accidental API usage
- Must explicitly pass `--no-dryrun` to enable real calls
- All real API calls are logged

---

## Cost Tracking & Budgeting

The LLM system includes comprehensive cost tracking to monitor token usage and API spending.

### Cost Tracker (`llm/cost_tracker.py`)

**Features:**
- Tracks token usage per API call (input/output tokens)
- Calculates costs using backend-specific pricing tables
- Aggregates statistics by backend and model
- Exports detailed cost reports to JSON
- Budget checking with over-budget alerts

**Pricing Table:**

| Backend | Model | Input ($/MTok) | Output ($/MTok) |
|---------|-------|----------------|-----------------|
| Claude API | claude-sonnet-4 | $3.00 | $15.00 |
| Claude API | claude-haiku | $0.25 | $1.25 |
| OpenRouter | gpt-4-turbo | $10.00 | $30.00 |
| OpenRouter | llama-3.1-70b | $0.35 | $0.40 |
| Simulation | dryrun | $0.00 | $0.00 |

**Usage:**

```python
from llm import CostTracker

tracker = CostTracker()

# Track a call
cost = tracker.track_call(
    model_name="claude-sonnet-4",
    tokens_in=250,
    tokens_out=150,
    backend="claude-api",
    metadata={"market_id": "m-123"}
)

# Aggregate session statistics
summary = tracker.aggregate_session()
print(f"Total cost: ${summary.total_cost_usd:.4f}")
print(f"Total tokens: {summary.total_tokens_in + summary.total_tokens_out:,}")

# Check budget
budget_status = tracker.check_budget(budget_usd=10.0)
if budget_status["over_budget"]:
    print(f"WARNING: Over budget by ${abs(budget_status['remaining_usd']):.2f}")

# Export report
tracker.export_cost_report(Path("state/llm_cost_report.json"))
```

**Output Format (`llm_cost_report.json`):**

```json
{
  "summary": {
    "session_start": "2025-11-18T12:00:00Z",
    "session_end": "2025-11-18T12:15:30Z",
    "total_calls": 42,
    "total_tokens_in": 10500,
    "total_tokens_out": 6300,
    "total_cost_usd": 0.126,
    "calls_by_backend": {"claude-api": 40, "simulation": 2},
    "calls_by_model": {"claude-sonnet-4": 40, "dryrun": 2},
    "cost_by_backend": {"claude-api": 0.126, "simulation": 0.0}
  },
  "detailed_calls": [
    {
      "timestamp": "2025-11-18T12:00:05Z",
      "model_name": "claude-sonnet-4",
      "backend": "claude-api",
      "tokens_in": 250,
      "tokens_out": 150,
      "cost_usd": 0.003,
      "metadata": {"market_id": "m-123", "category": "crypto"}
    }
  ]
}
```

---

## Evaluation & Calibration

The system evaluates LLM performance by comparing against the naive baseline model.

### Evaluator (`llm/evaluation.py`)

**Metrics:**

1. **Directional Agreement** - Do LLM and naive agree on action (buy_yes/buy_no/avoid)?
2. **Probability Delta** - Difference between LLM and naive fair probability estimates
3. **Edge Delta** - Difference in estimated edge (basis points)
4. **Confidence Alignment** - Does LLM confidence match edge size?
   - High confidence → large edge (|edge| >= 500 bps): **aligned**
   - High confidence → small edge (|edge| < 500 bps): **misaligned**
   - Medium confidence → 200-500 bps: **aligned**
   - Low confidence → <200 bps: **aligned**
5. **Calibration Score** - Percentage of predictions with aligned confidence-edge pairs

**Usage:**

```python
from llm import LLMEvaluator

evaluator = LLMEvaluator()

# Add comparison
evaluator.add_comparison(
    market_id="m-123",
    market_question="Will Bitcoin reach $100k?",
    market_category="crypto",
    llm_opinion={
        "fair_probability": 68.5,
        "confidence": "high",
        "edge_bps": 650,
        "action": "buy_yes"
    },
    naive_opinion={
        "fair_yes": 0.62,
        "edge": 0.0,
        "rec": "avoid",
        "notes": "Naive model baseline"
    }
)

# Generate report
report = evaluator.generate_report()
print(f"Agreement rate: {report.agreement_rate:.1%}")
print(f"Avg probability delta: {report.avg_probability_delta:.1f}%")
print(f"Calibration score: {report.calibration_score:.1%}")

# Export report
evaluator.export_report(Path("state/llm_evaluation_report.json"))
```

**Output Format (`llm_evaluation_report.json`):**

```json
{
  "report": {
    "generated_at": "2025-11-18T12:15:30Z",
    "total_comparisons": 42,
    "llm_available_count": 40,
    "fallback_count": 2,
    "agreement_rate": 0.75,
    "agreements": 30,
    "disagreements": 10,
    "avg_probability_delta": 5.2,
    "avg_edge_delta_bps": 120,
    "calibration_score": 0.85,
    "by_confidence": {
      "high": {
        "count": 15,
        "agreement_rate": 0.80,
        "avg_edge_delta_bps": 150,
        "confidence_alignment_rate": 0.93
      },
      "medium": {"count": 20, "agreement_rate": 0.75, "avg_edge_delta_bps": 100, "confidence_alignment_rate": 0.85},
      "low": {"count": 5, "agreement_rate": 0.60, "avg_edge_delta_bps": 50, "confidence_alignment_rate": 0.80}
    },
    "by_category": {
      "crypto": {"count": 20, "llm_available": 19, "agreement_rate": 0.79},
      "politics": {"count": 15, "llm_available": 14, "agreement_rate": 0.71},
      "sports": {"count": 7, "llm_available": 7, "agreement_rate": 0.71}
    }
  },
  "detailed_comparisons": [
    {
      "market_id": "m-123",
      "market_question": "Will Bitcoin reach $100k?",
      "market_category": "crypto",
      "llm_fair_probability": 68.5,
      "llm_edge_bps": 650,
      "llm_action": "buy_yes",
      "llm_confidence": "high",
      "naive_fair_probability": 62.0,
      "naive_edge_bps": 0,
      "naive_action": "avoid",
      "directional_agreement": false,
      "probability_delta": 6.5,
      "edge_delta_bps": 650,
      "confidence_edge_alignment": "aligned"
    }
  ]
}
```

**Interpreting Results:**

- **High agreement rate (>80%)**: LLM and naive converge, good calibration
- **Low agreement rate (<50%)**: LLM diverges from baseline, may find novel edges
- **High calibration score (>90%)**: LLM confidence matches edge estimates well
- **Low calibration score (<70%)**: LLM confidence miscalibrated, needs prompt tuning
- **Large probability deltas**: LLM sees opportunities naive model misses
- **Small probability deltas**: LLM agrees with baseline, conservative estimates

---

## Integrated Pipeline

The cost tracker and evaluator integrate seamlessly into `ho_llm_polymarket.py`:

```python
from llm import LLMMarketAnalyst, CostTracker, LLMEvaluator

# Initialize
analyst = LLMMarketAnalyst(dry_run=True)
cost_tracker = CostTracker()
evaluator = LLMEvaluator()

# Analyze markets with cost + eval tracking
results = analyze_markets_with_llm(markets, analyst, cost_tracker, evaluator)

# Export reports
cost_tracker.export_cost_report(Path("state/llm_cost_report.json"))
evaluator.export_report(Path("state/llm_evaluation_report.json"))

# Print summary
cost_summary = cost_tracker.aggregate_session()
eval_report = evaluator.generate_report()

print(f"Cost: ${cost_summary.total_cost_usd:.6f}")
print(f"Agreement: {eval_report.agreement_rate:.1%}")
print(f"Calibration: {eval_report.calibration_score:.1%}")
```

**Default Output Locations:**

- Cost report: `~/hands-off-out/state/llm_cost_report.json`
- Evaluation report: `~/hands-off-out/state/llm_evaluation_report.json`
- Alpha report: `~/hands-off-out/state/llm_alpha_report.json`

---

## Future Enhancements

1. **Ensemble voting** - Combine multiple LLM backends for better accuracy
2. **Real-time API calls** - Implement actual Claude/OpenRouter integration
3. **Local model support** - Run Llama locally for privacy/cost savings
4. **Prompt A/B testing** - Test different prompt variations
5. **Category-specific models** - Specialized analysis per category
6. **Historical outcome tracking** - Compare predictions to actual market resolutions
7. **Adaptive budgeting** - Dynamic budget allocation based on market value

---

## Contact & Support

For issues or questions about the LLM integration:
1. Check logs in console output
2. Verify API keys are set
3. Run tests to validate setup
4. Review this documentation

This is a **DRYRUN-only** system. All integration with live trading must be explicitly approved and thoroughly tested.
